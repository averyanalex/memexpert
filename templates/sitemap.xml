<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {% for meme in memes %}
  <url>
    <loc>https://memexpert.xyz/{{ meme.translations[0].language }}/{{ meme.slug }}</loc>
    {% for translation in meme.translations %}
    <xhtml:link
      rel="alternate"
      hreflang="{{ translation.language }}"
      href="https://memexpert.xyz/{{ translation.language }}/{{ meme.slug }}"
    />
    {% endfor %}
    <lastmod>{{ meme.lastmod }}</lastmod>
  </url>
  {% endfor %}
</urlset>